// user details
let user: [string, number] = ["iyynes", 25];

// accessing tuple elements
user[0].toLowerCase(); // string method
user[1].toFixed();    // number method
// its shows all the methods available for string and number types


// tuples are useful only when we have two values like a key and value pair
// more than make it more complex to understand